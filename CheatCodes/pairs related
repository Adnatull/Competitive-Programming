Total pairs where sum of each pair is not greater than k: (Pairs have to be positive)

if k=odd
  then pairs = (k-1)/2*(k-1)/2;
else if k=even
  then pairs = (k-2)/2*(k/2);


Total pairs among n people:
  pairs = n*(n-1)/2;


